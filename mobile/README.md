# Watermelon Banking Mobile App

This folder contains the Expo/React Native banking app:

- `banking-mobile`

The app calls the Next.js banking APIs. After deploying your own backend, update the `API_BASE_URL` constant in `banking-mobile/App.js`.
